server:
  port: 8080
spring:
  application:
    name: cloud-api
  cloud:
    config:
      profile: default
      label: master
      discovery:
        service-id: cloud-config
  boot:
    admin:
      url: http://localhost:8090