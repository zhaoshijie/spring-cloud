server:
  port: 8888
spring:
  application:
    name: cloud-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kasuganokaze/spring-cloud
          search-paths: config-properties
          username:
          password: