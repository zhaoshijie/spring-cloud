server:
  port: 8180
spring:
  application:
    name: cloud-api-amqp
  cloud:
    config:
      profile: default
      label: master
      discovery:
        service-id: cloud-config
  sleuth:
    sampler:
      percentage: 1