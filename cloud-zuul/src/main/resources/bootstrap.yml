spring:
  application:
    name: cloud-zuul
  cloud:
    config:
      profile: default
      label: master
      discovery:
        service-id: cloud-config
      username: config
      password: 123456